# Optional override. Leave unset in production; the built app uses the same backend origin.
# VITE_API_BASE=http://localhost:8000
# Optional legacy fallback for private/trusted builds only. Normal installs use admin login.
# VITE_API_TOKEN=replace-with-the-backend-token
