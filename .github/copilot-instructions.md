# Cloud Migration Console repository instructions

Follow `AGENTS.md` and read `docs/RELEASING.md` before release work. Do not change `VERSION` during ordinary development. For an explicitly requested release, use `scripts/prepare-release.sh` and never publish or move a release tag manually outside the documented, CI-validated process.
